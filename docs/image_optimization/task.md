# コンテンツ管理の最適化とメモリクラッシュ対策

`main.json` の Base64 による肥大化を解消し、画像をファイルベースに移行することでメモリ負荷を軽減しました。

- [x] 既存のBase64画像データを抽出してファイルとして保存するスクリプトを作成・実行
- [x] `contents-manager.html` に画像リサイズ・圧縮ロジックを導入
- [x] `contents-manager.html` の保存処理を修正（画像ファイルを個別にリポジトリに保存）
- [x] `gcp-save-api` をバイナリ形式の保存に対応するよう修正
- [x] Cloud Functions へのデプロイ実行
- [x] 最終的な挙動確認
